# Lodyland (Click & Collect) – MVP

Objectif: jeu 2D "click-to-collect" jouable navigateur (Flask backend).
BABY STEPS — on démarre en minimal absolu.

## Démarrer en local
```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install flask
python run.py
